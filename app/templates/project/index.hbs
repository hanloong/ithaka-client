<div class='show-project'>
  <div class='heading'>
    <h2>{{model.project.name}}</h2>
    <div class='content'>
      {{model.project.description}}
    </div>
  </div>

  <div class='ideas'>
    {{#if model.ideas}}
      {{#each idea in model.ideas}}
        <div class='idea'>
          {{#link-to 'idea.index' idea.project idea}}
            <h2>{{idea.name}}</h2>
          {{/link-to}}
          <div class='content'>
            <p>Score: {{idea.score}}</p>
            <p>{{{idea.description}}}</p>
          </div>
        </div>
      {{/each}}
    {{else}}
      <div class='idea'>
        <h2>No ideas yet</h2>
        <div class='content'>
          <p>Maybe you should add one now.</p>
          {{#link-to 'ideas.new'}}
            Create new idea
          {{/link-to}}
        </div>
      </div>
    {{/if}}
  </div>

  <div class="search">
    <h2>Search and Sort</h2>
  </div>
</div>
